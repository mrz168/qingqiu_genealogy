---
stepsCompleted: [1, 2]
inputDocuments: []
session_topic: '家族族谱系统 - 功能需求与软件架构设计'
session_goals: '探索系统所需功能清单；设计合理的软件架构方案'
selected_approach: 'ai-recommended + progressive-flow'
techniques_used: ['Mind Mapping', 'Morphological Analysis', 'SCAMPER Method', 'Decision Tree Mapping']
ideas_generated: 138
context_file: ''
---

# Brainstorming Session Results

**Facilitator:** BobCheung
**Date:** 2026-03-06

## Session Overview

**Topic:** 家族族谱系统 - 功能需求与软件架构设计

**Goals:** 
- 探索该系统需要哪些功能
- 设计合理的软件架构

### Session Setup

_用户已明确主题：家族族谱系统。目标聚焦于功能需求挖掘与软件架构设计。_

## Technique Selection

**Approach:** AI-Recommended + Progressive Technique Flow
**Journey Design:** 从广泛探索到聚焦落地的系统性创意旅程

**Progressive Techniques:**

- **Phase 1 - 发散探索:** Mind Mapping（思维导图）— 以「家族族谱系统」为中心，自由分支功能与用户场景
- **Phase 2 - 模式识别:** Morphological Analysis（形态学分析）— 系统化梳理功能维度与架构参数组合
- **Phase 3 - 概念深化:** SCAMPER Method（SCAMPER 法）— 对核心功能与架构进行替代、组合、适配等改进
- **Phase 4 - 行动规划:** Decision Tree Mapping（决策树映射）— 将架构决策与实施路径可视化

**AI Rationale:** 族谱系统兼具产品功能与系统架构，需要先发散再收敛。思维导图适合功能发散；形态学分析适合多维度组合；SCAMPER 适合在已有基础上迭代；决策树适合架构选型与实施规划。

---

## Technique Execution Results

### Phase 1: Mind Mapping（思维导图）— 进行中

---

## 功能需求记录（Phase 1 产出）

> 以下为 Phase 1 思维导图阶段已捕获的想法，供后续阶段参考，避免遗漏。

---

### 一、用户原有想法

#### 1. 角色与权限

| 角色 | 能力 |
|------|------|
| **管理员** | 族谱创建与管理；添加、修改家族成员 |
| **家族成员** | 查看族谱；修改自己的生平（修改后需审批，通过后生效） |

#### 2. 成员数据结构

- **基本信息**：姓名、出生日期、照片
- **生平**：个人经历、事迹等

#### 3. 关系网

- 家族成员之间的关系网由**系统自动生成**（基于录入的关系数据）

#### 4. 审批流程

- 成员修改自己的生平 → 提交 → 系统自动发起审批 → 审批通过后修改生效

---

### 二、补充与延伸想法

#### 1. 权限与角色扩展

- **子管理员/分支管理员**：按房支、地区划分，负责本支数据的维护与初审
- 明确审批人：族长、主管理员或分支管理员

#### 2. 成员信息扩展

- 逝世日期、籍贯、职业
- 配偶、子女等亲属关系
- 迁居记录、重要事件时间线

#### 3. 关系网细化

- 血缘关系 vs 姻亲关系的区分
- 过继、收养等特殊关系的表示
- 多配偶（历史场景）的建模

#### 4. 审批流程细化

- 驳回后是否允许修改重提
- 修改历史记录（版本追溯）

#### 5. 多代与时间维度

- 按世代、年代筛选视图
- 时间轴视图
- 生卒年、重大事件时间线

#### 6. 多媒体与故事

- 老照片、老物件、口述历史、视频
- 成员故事、家族大事记

#### 7. 隐私与可见性

- 在世/去世成员的可见性控制
- 敏感信息（如非婚生子女）的权限控制

#### 8. 协作与传承

- 多人共同维护族谱
- 邀请族人加入系统
- 下一代如何接手管理权

#### 9. 展示与分享

- 族谱导出（PDF、图片）
- 家族树打印
- 分享链接给族人

#### 10. 搜索与统计

- 按姓名、辈分、地区搜索
- 世代统计、人口统计
- 迁徙路径可视化

#### 11. 移动端

- 手机端查看、编辑
- 拍照上传老照片
- 长辈在手机上参与

#### 12. 多族谱与跨家族

- 一个家族多个分支
- 不同家族（如联姻）之间的关联

---

### 三、技术架构决策（Phase 1 技术分支）

#### 1. 部署与访问

| 维度 | 决策 |
|------|------|
| 部署模式 | 单家族自用 |
| 离线支持 | 无需 |
| 多租户 | 无需 |

#### 2. 技术栈

| 层级 | 选型 |
|------|------|
| 前端 | Web 为主，移动端后期考虑 |
| 后端 | Django + Django-Ninja（API） |
| 并发 | 无高并发场景 |
| 数据库 | PostgreSQL |
| 文件存储 | 本地 + 对象存储（用户可配置） |

#### 3. 关系网

| 维度 | 决策 |
|------|------|
| 关系类型 | 父子、配偶、兄弟等中国家族关系 |
| 更新策略 | 每增/改/删成员后生成或更新一次 |
| 规模 | 单族谱最大可能达百万级 |

#### 4. 审批数据

- 审批流程、审批人、审批记录均使用 PostgreSQL 存储

---

### 四、推荐方案（核心字段、关系实体、版本管理）

#### 1. 成员核心字段推荐

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID | 主键 |
| `name` | 字符串 | 姓名（含曾用名、字、号等可拆分或扩展） |
| `gender` | 枚举 | 男/女/未知 |
| `birth_date` | 日期 | 出生日期（可为近似，如「约 1900 年」） |
| `death_date` | 日期 | 逝世日期，可空 |
| `birth_place` | 字符串 | 出生地/籍贯 |
| `biography` | 文本 | 生平 |
| `avatar` | 文件引用 | 照片/头像 |
| `generation` | 整数 | 世代（如第几世），便于排序与筛选 |
| `branch` | 字符串 | 房支/分支，可空 |
| `is_alive` | 布尔 | 是否在世（影响隐私与可见性） |
| `created_at` / `updated_at` | 时间戳 | 审计 |
| `extra` | JSONB | 扩展字段，支持自定义属性 |

**扩展字段（extra）示例：** 职业、学历、配偶姓名、迁居记录、重要事件等，可按家族习惯配置。

#### 2. 关系实体推荐

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID | 主键 |
| `genealogy_id` | 外键 | 所属族谱 |
| `person_from_id` | 外键 | 关系主体 |
| `person_to_id` | 外键 | 关系客体 |
| `relation_type` | 枚举 | 关系类型（见下表） |
| `order` | 整数 | 同类型多关系时的顺序（如多子女） |
| `start_date` / `end_date` | 日期 | 关系有效时间（如婚姻起止），可空 |
| `extra` | JSONB | 扩展（如过继原因、收养说明等） |

**关系类型（relation_type）建议：**

| 值 | 说明 | 方向 |
|----|------|------|
| `father` | 父亲 | person_from=父, person_to=子 |
| `mother` | 母亲 | person_from=母, person_to=子 |
| `spouse` | 配偶 | 双向，可多条（多配偶） |
| `son` / `daughter` | 子女 | 可由 father/mother 推导，也可显式存储 |
| `adopted` | 收养 | person_from=养父母, person_to=养子女 |
| `foster` | 过继 | person_from=过继方, person_to=被过继者 |

**说明：** 父子、母子、配偶为最基础关系；兄弟、堂兄弟等可由父子关系推导，也可视需求增加 `sibling` 等类型以加速查询。

#### 3. 族谱版本管理推荐

| 概念 | 说明 |
|------|------|
| **版本快照** | 每次「定稿/出版」时创建版本，保存当时成员与关系的快照或引用 |
| **版本表** | `id`, `genealogy_id`, `version_number`, `name`（如「2025 年续修版」）, `snapshot_at`, `created_at` |
| **快照策略** | 方案 A：冗余存储关键表快照（适合百万级，查询简单）；方案 B：仅存版本时间点 + 增量变更日志（省空间，需按时间回放） |
| **当前工作版** | 日常编辑针对「当前草稿」，定稿时生成新版本，草稿清空或进入下一轮编辑 |

**推荐：** 百万级规模下，版本表只存元数据；具体数据通过「版本时间点 + 软删除/状态」在成员表、关系表中标记，或采用「版本分支表」存储差异，避免全量快照占用过大。

#### 4. 审批相关表结构（PG）

| 表 | 用途 |
|----|------|
| `approval_flow` | 审批流程定义（如：成员生平修改 → 需族长审批） |
| `approval_request` | 单次审批请求（关联成员、修改内容、申请人、状态） |
| `approval_step` | 审批步骤（多级审批时使用） |
| `approval_record` | 审批记录（谁在何时通过/驳回，意见） |

---

### 五、百万级关系网更新策略建议

- **同步 vs 异步**：百万级建议**异步更新**，增删改成员后投递任务到队列，后台生成/更新关系网，避免接口超时。
- **增量更新**：若关系网可增量计算（如仅局部子树变化），优先增量；否则全量重算需控制频率与资源。
- **缓存/物化**：关系网结果可物化到表或缓存，前端按需加载分页或子图，避免每次实时计算。

---

### 六、安全与认证（Phase 1 安全分支）

#### 1. 用户身份与登录

| 维度 | 决策 |
|------|------|
| 登录方式 | 手机号、邮箱（每个成员自动生成 UUID 作为账号） |
| 社交登录 | 支持微信、QQ 登录 |
| 邀请制 | 支持；家族成员均可发起邀请，需管理员或子管理员审批通过 |
| 账号对应 | 成员与系统账号必须一一对应 |

#### 2. 权限边界

| 角色 | 权限 |
|------|------|
| **管理员** | 拥有系统所有权限 |
| **子管理员** | 由管理员分配权限并指定管理范围 |
| **成员** | 可查看家族成员信息、家庭树；可修改自己的信息 |

**说明：** 无需按在世/去世或亲疏控制可见性；敏感信息无需对部分成员隐藏。

#### 3. 数据安全

| 需求 | 决策 |
|------|------|
| 操作日志 | 需要 |
| 定期备份 | 需要；备份策略由用户自行设置 |
| 数据导出 | 需要（支持迁移） |

---

### 七、前端交互体验（Phase 1 前端分支）

#### 1. 家族树展示

| 维度 | 决策 |
|------|------|
| 展示形式 | 树状图 + 世系表（两者均需支持） |
| 百万级加载 | 按分支懒加载；**登录用户访问时自动聚焦到该用户** |
| 其他加载方式 | 支持按世代分页；支持先展示概览再下钻 |
| 交互能力 | 支持缩放、拖拽、搜索、定位 |

#### 2. 编辑体验

| 维度 | 决策 |
|------|------|
| 添加成员 | 支持在树图上直接点选父/母/配偶建立关系 |
| 批量导入 | 支持（Excel/模板） |
| 生平编辑 | 支持富文本及 Markdown |

#### 3. 移动端（后期）

| 维度 | 决策 |
|------|------|
| 形态 | 同时支持小程序 + App |
| 适老化 | 需匹配长辈使用（大字体、简化操作等） |

---

### 八、API 设计（Phase 1 API 分支）

#### 1. 接口风格

| 维度 | 决策 |
|------|------|
| 风格 | RESTful |
| 复杂关联查询 | 需要（如一次获取成员 + 关系 + 生平） |
| 版本控制 | 需要（如 `/api/v1/`） |
| 分页/排序/筛选 | 需提供推荐方案 |

**分页/排序/筛选推荐：**

| 能力 | 推荐约定 |
|------|----------|
| 分页 | `?page=1&page_size=20` 或 `?offset=0&limit=20` |
| 排序 | `?order_by=generation,name`，`?order=asc` / `desc` |
| 筛选 | `?generation=5&branch=长房`，多条件组合 |

#### 2. 认证与文档

| 维度 | 决策 |
|------|------|
| 认证 | 采用 Django 推荐方案（Session / JWT，与 Django-Ninja 兼容） |
| 限流 | 按 Django 生态推荐实现 |
| 文档 | 建议使用 OpenAPI/Swagger（Django-Ninja 内置支持） |

---

### 九、通知与消息（Phase 1 通知分支）

#### 1. 审批通知

| 场景 | 渠道 |
|------|------|
| 审批人收到待审批 | 站内信、邮件、短信、微信 |
| 申请人收到审批结果 | 同上 |

#### 2. 邀请与公告

| 场景 | 渠道 |
|------|------|
| 被邀请人收到邀请通过 | 短信或微信 |
| 家族活动/公告 | 需要（推送渠道待定） |

#### 3. 提醒

| 类型 | 决策 |
|------|------|
| 待办提醒 | 需要（如「您有 X 条待审批」） |
| 纪念日提醒 | 需要（生日、忌日等） |

---

### 十、导入导出（Phase 1 导入导出分支）

#### 1. 导出

| 维度 | 决策 |
|------|------|
| 版式 | 世系表、树状图、成员名录（均需支持） |
| 格式 | PDF、图片；需支持结构化导出（JSON、Excel） |

#### 2. 导入

| 维度 | 决策 |
|------|------|
| 批量导入 | 支持；模板字段见下方推荐 |
| 其他族谱软件 | 无需支持（如 GEDCOM） |
| 导入流程 | 需支持预览、校验、错误报告 |

**批量导入 Excel 模板字段推荐：**

| 列名 | 必填 | 说明 |
|------|------|------|
| 姓名 | ✓ | |
| 性别 | ✓ | 男/女 |
| 出生日期 | | 格式：YYYY-MM-DD 或 YYYY |
| 逝世日期 | | 同上 |
| 籍贯/出生地 | | |
| 父亲姓名 | | 用于建立父子关系，需与已有成员匹配 |
| 母亲姓名 | | 用于建立母子关系 |
| 配偶姓名 | | 多个用分号分隔 |
| 世代 | | 整数，如第几世 |
| 房支 | | |
| 生平 | | 支持多行 |
| 照片URL | | 可选，后续上传 |

---

### 十一、错误处理与异常（Phase 1 错误处理分支）

#### 1. API 错误响应

| 维度 | 决策 |
|------|------|
| 错误结构 | 需提供推荐方案 |
| 状态码 | 需提供规范 |
| 校验失败 | 需返回具体字段错误 |

**错误响应结构推荐：**

```json
{
  "code": "VALIDATION_ERROR",
  "message": "请求参数校验失败",
  "details": [
    { "field": "name", "message": "姓名不能为空" },
    { "field": "birth_date", "message": "出生日期格式错误，应为 YYYY-MM-DD" }
  ]
}
```

**HTTP 状态码规范推荐：**

| 状态码 | 场景 |
|--------|------|
| 400 | 请求参数错误（格式、类型） |
| 401 | 未认证 / Token 失效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 冲突（如关系冲突、重复提交） |
| 422 | 业务校验失败（如关系循环、逻辑错误） |
| 500 | 服务器内部错误 |

#### 2. 业务异常

| 场景 | 决策 |
|------|------|
| 关系冲突 | 需提供提示推荐 |
| 循环引用 | 需提供检测与拦截推荐 |
| 删除有关联的成员 | 不允许删除 |
| 成员状态标识 | 灰色 badge：已离世；红色 badge：已逐出/叛出家族 |

**关系冲突提示推荐：**  
「无法建立关系：A 与 B 已存在父子关系，不能同时建立反向关系。」或「该关系与现有关系冲突，请检查后重试。」

**循环引用检测与拦截推荐：**  
建立/修改关系时，从新关系出发做可达性检测（如 BFS/DFS），若存在 A→B→C→…→A 的环路则拒绝操作，并返回：「检测到关系循环（如 A→B→C→A），请修正后重试。」

---

### 十二、部署与运维（Phase 1 部署分支）

#### 1. 部署

| 维度 | 决策 |
|------|------|
| 部署方式 | Docker |
| 健康检查 | 需要（如 `/health` 或 `/api/health`） |

#### 2. 日志

| 维度 | 决策 |
|------|------|
| 切割 | 按天切割 |
| 保留 | 365 天 |

---

### 十三、边界与异常场景（Phase 1 边界分支）

#### 1. 数据边界

| 场景 | 决策 |
|------|------|
| 同名同姓 | 使用出生日期辅助标识 |
| 出生日期不详 | 需提供存储与展示推荐 |
| 无子女/无父母 | 需提供边界处理推荐 |

**出生日期不详存储与展示推荐：**

| 存储 | 展示 |
|------|------|
| 精确：`birth_date = "1900-01-15"` | 「1900 年」或「1900 年 1 月」 |
| 约：`birth_date_approx = "约1900"`，`birth_date_precision = "year"` | 「约 1900 年」 |
| 朝代：`birth_era = "清末"`，存于 `extra` | 「清末」 |
| 不详：`birth_date = null`，`birth_date_known = false` | 「不详」 |

**无子女、无父母等边界推荐：**

| 场景 | 处理 |
|------|------|
| 无子女 | 允许，关系表中无对应 `person_to` 记录 |
| 无父母（始祖） | 允许，`person_from` 为空或指向虚拟「始祖」节点 |
| 配偶已故/未录入 | 配偶关系可单边存在，`person_to` 可空或为占位 |
| 过继/出继 | 可保留生父母关系 + 养父母关系，用 `relation_type` 区分 |

#### 2. 并发与一致性

| 场景 | 决策 |
|------|------|
| 多人同时编辑同一成员 | 不允许（编辑时加锁或占位，他人只读） |
| 关系网异步更新中 | 前端展示上一版结果，使用「计算中」遮罩 |
